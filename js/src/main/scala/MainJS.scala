import acrosscompilers.AcrossCompilersShared

object MainJS {
  def main(args: Array[String]): Unit = {
    println("JSJSJS running from directly within the JS folder")

    var array_Coords = AcrossCompilersShared.transferCoords()

    
  }
}
